// This project serves a static portfolio directly from /app/frontend/public/
// (index.html + styles.css + script.js). The React app renders nothing so it
// doesn't overlay the static content.
function App() {
    return null;
}

export default App;
